/**
 * @startingPoint section="Components" subtitle="Initial avatar — olive circle" viewport="120x120"
 */
export interface AvatarProps {
  name: string;
  size?: number;
  color?: string;
}

/**
 * @startingPoint section="Components" subtitle="Team member pill (Meet Your Team)" viewport="260x70"
 */
export interface StaffPillProps {
  name: string;
  role: string;
}
