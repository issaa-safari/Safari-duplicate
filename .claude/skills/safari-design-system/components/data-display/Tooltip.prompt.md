Small hover tooltip for icon-only buttons or truncated labels in admin tables.

```jsx
<Tooltip label="Edit this departure"><IconButton icon={<Pencil size={16} />} /></Tooltip>
```

Dark Bush background, appears above the trigger, no arrow (kept minimal).
