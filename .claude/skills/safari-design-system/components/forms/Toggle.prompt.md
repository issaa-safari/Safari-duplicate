Small switch used in the admin dashboard for on/off settings (notifications, visibility).

```jsx
<Toggle checked={enabled} onChange={setEnabled} label="Show on public site" />
```

Olive fill when on, warm-grey track when off. Always pair with a visible label — never use standalone in a data table without a column header.
