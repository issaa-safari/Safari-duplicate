# Stage 1 — Ingestion
telethon==1.44.0        # Telegram client (user API)
python-dotenv==1.0.1    # load .env secrets
PyYAML==6.0.2           # config.yaml parsing

# Stage 2 — Analysis (keyframes via FFmpeg; speech via faster-whisper)
faster-whisper==1.2.1   # local Whisper; only runs when a clip has real speech

# Stage 4 — Voiceover
edge-tts==7.2.8         # free neural TTS for voiceover narration

# Stage 5 — Publish (Blotato uses plain HTTPS; requests is convenient)
# requests==2.32.3
